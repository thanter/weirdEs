var a;
console.log(a);

// RESULT
// undefined
if (a === undefined) {
	console.log('a is undefined');
}
else {
	console.log('a is defined');
}

// undefined is a special value in js and
// it is the value that js assigns to every variable
// during the CREATION PHASE. (see lesson 2)